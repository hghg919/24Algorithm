#include "Sort.h"
#include <algorithm>
#include <iostream>

void bubbleSort(int list[], int n)
{
	for (int i = n - 1 : i > 0 : i--)
	{
		for (int j = 0; j < ? ; j++)
		{
		
			if(list[j] > list[j+1])
			{
				std::swap(list[j], list[j + 1]);
			}
		}

	}

}

void BubbleExampe()
{
	int list[] = {};
}